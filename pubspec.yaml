name: surplace_pos
description: Simple POS app
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

  # Yazıcı için sadece PLUS sürümü (eskisini asla ekleme!)
  esc_pos_utils_plus: ^2.0.1+6

  # Ayarlar/IP saklamak için
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
