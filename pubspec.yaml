name: surplace_pos
description: Simple POS app (Commande Sur Place)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  intl: ^0.19.0
  collection: ^1.18.0
  shared_preferences: ^2.2.2

  # Yalnızca ORİJİNAL util ve ona uygun yazıcı paketi
  esc_pos_printer: ^4.0.4
  esc_pos_utils: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
