name: surplace_pos
description: Simple POS app
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2

  # Yalnızca PLUS sürüm (diğerini kesinlikle eklemiyoruz)
  esc_pos_utils_plus: ^2.0.1+6
  ping_discover_network: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
